import { SettingsScreen } from '@/features/settings/SettingsScreen';

/** Thin route. All logic lives in src/features/settings (CLAUDE.md §3). */
export default function Settings() {
  return <SettingsScreen />;
}
