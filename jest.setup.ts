// Global Jest setup.
//
// Stage 1 will add the mocks needed to test src/core/i18n/format.ts and the
// RTL bootstrap (expo-localization, I18nManager). Kept empty on purpose so
// Stage 0 has no behaviour to explain.

export {};
